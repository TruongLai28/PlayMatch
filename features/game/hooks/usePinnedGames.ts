import { usePinnedCard } from '../PinnedCardProvider'

// Re-export the pinned card hook with a more descriptive name
export const usePinnedGames = usePinnedCard

// You can add more game-related hooks here in the future
// export const useGameSearch = () => { ... }
// export const useGameRecommendations = () => { ... }